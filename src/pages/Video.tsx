import { VideoView } from "../widgets/VideoView";

const Video = () => {
  // Add url of mjpeg stream to display here
    return (
      <>
      <VideoView url=""
      />
      </>
    );
  };
  
  export default Video;